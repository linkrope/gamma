MODULE Error;
CONST M := 10, N = 100 X = 10;
VAR , a, b, c;

PROCEDURE P;
BEGIN
    s := 0; a = 5 * (b - 1 END;

BEGIN
    > a > b;
    WHILE a DO
        BEGIN > b; - c := 0;
        WHILE a > 0 BEGIN
            IF ODD [a c := c * - b;
            b := 2 * b; a := a *2
        END;
        P := 0; P; 666;
    END .
